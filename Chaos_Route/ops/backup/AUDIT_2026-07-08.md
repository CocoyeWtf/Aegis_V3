# Audit de la sauvegarde existante — 2026-07-08 (STIME B1)

## Constat

| Élément | État constaté |
|---|---|
| Cron | `0 3 * * * /opt/chaos-route/backup.sh >> /opt/chaos-route/backups/backup.log 2>&1` |
| Script `backup.sh` | **ABSENT du serveur** (probablement perdu lors d'un nettoyage/checkout) |
| Journal `backups/backup.log` | 7,9 Ko de lignes `backup.sh: not found` — **la sauvegarde nocturne échouait silencieusement chaque nuit** |
| Dumps présents | Uniquement 3 dumps **manuels** pré-opérations (19/06, 19/06, 22/06), en clair, sur le même disque |
| Externalisation | Aucune |
| Chiffrement | Aucun |

**Conclusion : avant remédiation, il n'existait aucune sauvegarde automatique
fonctionnelle.** Le RPO réel était « depuis le dernier dump manuel » (16 jours
au moment de l'audit).

## Remédiation appliquée (même jour)

1. Ancien cron remplacé par `/root/ops/backup_chaos.sh` (02:30, journal
   `/var/log/chaos_backup.log`) — pg_dump → chiffrement age → rétention 14 j
   locale. Cf. [README.md](README.md).
2. Sauvegarde exécutée immédiatement + **test de restauration réel** dans un
   conteneur jetable (résultat consigné dans [RESTORE_LOG.md](RESTORE_LOG.md)).
3. Clé privée age retirée du serveur (coffre Dominic) ; seule la clé publique
   reste sur le VPS.
4. Reste ouvert (décision D) : externalisation S3 UE (`S3_BUCKET` à renseigner
   dans `/root/.chaos_backup.env`) — le script journalise un avertissement de
   non-conformité tant qu'elle n'est pas configurée.
5. Les 3 dumps manuels en clair seront supprimés après confirmation que la
   chaîne chiffrée fonctionne (ils datent de juin et sont supersédés).
